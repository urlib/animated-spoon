#include "bits/stdc++.h"
using namespace std;
int main()
{
    ifstream input;
    input.open("std.in");
    ofstream output;
    output.open("std.out");
    input.sync_with_stdio(0);
    output.sync_with_stdio(0);
    input.tie(0);
    output.tie(0);
    return 0;
}